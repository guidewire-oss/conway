.claude/
__pycache__/
*.pyc
.DS_Store
# server runtime artifacts
server/store.json
server/conway
conway-server
# process pids/logs from the Makefile
.run/
# local-only helper scripts and env (Makefile is the committed interface)
scripts/local/
# downloaded transcripts / scratch media (not source)
*.vtt

# local scratch (contains secrets) — never commit
pd.txt
server/game-state.json

# user-provided planning data (contains internal names) — keep out of git
project_planning.xlsx
project_planning_v2.xlsx
GWCP VPMOM FY 27 Planning.xlsx

# internal open-source readiness plan — not for the public repo
OSS_READINESS_PLAN.md

# real org data — regenerate your own via data/extract.py + the scripts/ pipeline
# (legacy v1 mining pipeline; the demo/seed dataset lives in server/db/seed/ now)
data/*.jsonl
data/*.json
data/*.txt
data/mining_summary.md
pod-directory*.csv

# internal planning notes and speaker decks tied to real people/teams
requester-tiers.md
flow-rules-gwcp.html
flow-rules-gwcp-notes.md

# AI session continuity notes — not for external contributors
claude-progress.md

# Guidewire-internal Atmos/Oort/KubeVela deployment (real governance labels,
# real hostnames) — kept locally for internal use, not part of the OSS repo.
# Open-source deployment is docker-compose.yml at the repo root instead.
deploy/

# real deploy-target env values (internal hostnames, governance labels) —
# source this from .envrc locally; never commit it
.envrc.local
