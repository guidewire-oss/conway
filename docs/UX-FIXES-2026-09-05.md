# Planning and execution usability changes — 2026-09-05

This implements the actionable findings in `UX-REVIEW-2026-09-05.md`.
The original review is a historical observation of the earlier checkout;
its source line numbers refer to that checkout. Requirements and decisions
for this change live in `specs/017-planning-and-execution-usability.md`.

| Review | Result | Evidence |
|---|---|---|
| F01 | Separate execution workspace with snapshot provenance, binding coverage, per-team actuals, scope changes, variance, conditional remaining work, buffer risk, adherence, calibration and recorded decisions. Missing evidence remains explicit. | `server/planning/actuals_test.go`, `server/execution_integration_test.go`, `tests/execution-ui.test.mjs` |
| F02 | Report distinguishes target misses from period fit and incomplete evidence. | `tests/report.test.mjs`, `tests/planning-ux.test.mjs` |
| F03 | Home separates high load from overcapacity using uncapped measured load; failed/synthetic/missing evidence is qualified. Hygiene totals come from whole-snapshot aggregates. | `tests/shell-ux.test.mjs`, `server/execution_integration_test.go` |
| F04 | Autosave/pending/failure feedback, independent scenario copies, explicit apply/agreement actions, visible pre-drop values and multiple timeline undo entries. Failed undo retains its history. | `tests/drag.test.mjs`, `tests/timeline-undo.test.mjs`, `tests/proposal-navigation.test.mjs`, database integration |
| F05 | Commitment view default, remembered views, collapsed populated setup, permission-aware routes and history, responsive plan navigation. | `tests/proposal-navigation.test.mjs`, `tests/browser/planning-ux.mjs` |
| F06 | Remedies preview before/after commitments and apply with an input fingerprint and database concurrency guard. | `server/planning/proposals_test.go`, `server/proposalhandlers_test.go`, database integration |
| F07 | Calendar labels, persistent independent filters, keyboard selection, precise timeline edits and team review. | `tests/planning-ux.test.mjs`, `tests/timeline.test.mjs`, browser workflow |
| F08 | Unified Help, native manual links, iframe Escape, search, narrow contents, remembered role guidance and corrected model explanations. | `tests/shell-ux.test.mjs`, `app/docs.html` |

Empty or unrecognized snapshot statuses also withhold progress and dependent
risk/forecast values; known issue counts remain available.

Additional review corrections cover named/focusable dialogs, dynamic focus
containment, delayed-response guards, escaped shared roster names, meaningful
control labels, and visible import/roster/snapshot failures. The manual explains
facilitator action points, duration, learning purpose and conditional forecasts.

## Validation observed in this session

These command outputs record the September 5 run. Counts describe that revision,
not the current test suite; subsequent review added independent regressions.

RAN `make check` with the installed Go/linter/Node binaries on PATH,
`GOCACHE` and `GOLANGCI_LINT_CACHE` in temporary directories, and
`CONWAY_TEST_DATABASE_URL` pointing to isolated PostgreSQL. Output excerpts:

```text
selftest: 159 passed, 0 failed
ok  conway/server 11.390s
0 issues.
tests 336
pass 336
fail 0
workflow-lint: OK (2 recipe(s))
```

The command exited 0. Go race tests included the real database tests, covering
migration 0020, access scoping, immutable agreements, scenario independence,
append-only decisions, uncapped issue counts and remedy compare-and-set writes.

RAN `node tests/browser/planning-ux.mjs` against the isolated test server with
its test credentials supplied in environment variables. Actual output:

```json
{"defaultOrder":true,"setupCollapsed":true,"routeRestored":true,"filterPreserved":true,"preciseEditUndo":true,"baselineValidation":true,"executionEvidence":true,"scenarioIndependent":true,"mobileOverflow":false,"pageErrors":[]}
```

The checked-in browser harness and setup instructions are in
`tests/browser/README.md`. Separate Chrome fixture checks exercised execution
failure/draft persistence and Help at 360px, including native links, search,
iframe Escape, role preference, modal focus and no page errors. These fixture
checks are supporting observations, not claims of live Jira integration.

## Capability limits

No representative-user study or production Jira import was performed.
Execution consumes accessible imported snapshots. Where transition history or
resolution timestamps are missing, the UI labels inference or unknown values;
conditional remaining-work dates assume the agreed rate and child-count proxy.
The review record does not send reminders or change Jira. Delivery improvement
and task-time targets require subsequent user research; they are not measured
outcomes of this implementation. Nothing was pushed or published.
